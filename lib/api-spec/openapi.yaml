openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: Auth
    description: Browser and mobile authentication endpoints.
  - name: profile
    description: User fitness profile operations
  - name: checkins
    description: Daily check-in operations
  - name: workouts
    description: External workout logging
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /auth/user:
    get:
      tags: [Auth]
      operationId: getCurrentAuthUser
      summary: Get the currently authenticated user
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
        - $ref: '#/components/parameters/SessionCookie'
      responses:
        '200':
          description: Auth status resolved successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUserEnvelope'
  /login:
    get:
      tags: [Auth]
      operationId: beginBrowserLogin
      summary: Start the browser OIDC login flow
      parameters:
        - name: returnTo
          in: query
          required: false
          schema:
            type: string
      responses:
        '302':
          description: Redirect to the OIDC authorization endpoint.
  /callback:
    get:
      tags: [Auth]
      operationId: handleBrowserLoginCallback
      summary: Complete the browser OIDC login flow
      parameters:
        - name: code
          in: query
          required: false
          schema:
            type: string
        - name: state
          in: query
          required: false
          schema:
            type: string
      responses:
        '302':
          description: Redirect to the returnTo path on success or /api/login on failure.
  /logout:
    get:
      tags: [Auth]
      operationId: logoutBrowserSession
      summary: Clear the session and begin OIDC logout
      responses:
        '302':
          description: Redirect to the OIDC provider logout URL.
  /mobile-auth/token-exchange:
    post:
      tags: [Auth]
      operationId: exchangeMobileAuthorizationCode
      summary: Exchange a mobile OIDC code for a session token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MobileTokenExchangeRequest'
      responses:
        '200':
          description: Session created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MobileTokenExchangeSuccess'
        '400':
          description: Missing or invalid required fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Token exchange failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /mobile-auth/logout:
    post:
      tags: [Auth]
      operationId: logoutMobileSession
      summary: Delete a mobile session token
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      responses:
        '200':
          description: Logout processed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogoutSuccess'
  /profile:
    get:
      tags: [profile]
      operationId: getUserProfile
      summary: Get the current user's fitness profile
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      responses:
        '200':
          description: User profile retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '401':
          description: Unauthorized.
    put:
      tags: [profile]
      operationId: updateUserProfile
      summary: Create or update the current user's fitness profile
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserProfileRequest'
      responses:
        '200':
          description: Profile updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '401':
          description: Unauthorized.
  /checkins:
    post:
      tags: [checkins]
      operationId: createDailyCheckIn
      summary: Create or update today's daily check-in
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCheckInRequest'
      responses:
        '200':
          description: Check-in saved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyCheckIn'
        '400':
          description: Missing required fields.
        '401':
          description: Unauthorized.
  /checkins/today:
    get:
      tags: [checkins]
      operationId: getTodayCheckIn
      summary: Get today's check-in for the current user
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      responses:
        '200':
          description: Today's check-in or null.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/DailyCheckIn'
                  - type: 'null'
        '401':
          description: Unauthorized.
  /workouts/external:
    post:
      tags: [workouts]
      operationId: logExternalWorkout
      summary: Log an external workout (manual or screenshot)
      parameters:
        - $ref: '#/components/parameters/AuthorizationSessionHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExternalWorkoutRequest'
      responses:
        '200':
          description: Workout logged.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalWorkout'
        '400':
          description: Missing required fields.
        '401':
          description: Unauthorized.
components:
  parameters:
    AuthorizationSessionHeader:
      name: Authorization
      in: header
      required: false
      schema:
        type: string
    SessionCookie:
      name: sid
      in: cookie
      required: false
      schema:
        type: string
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    AuthUser:
      type: object
      required: [id]
      properties:
        id:
          type: string
        email:
          type: string
          nullable: true
        firstName:
          type: string
          nullable: true
        lastName:
          type: string
          nullable: true
        profileImageUrl:
          type: string
          nullable: true
    AuthUserEnvelope:
      type: object
      required: [user]
      properties:
        user:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/AuthUser'
            - type: 'null'
    MobileTokenExchangeRequest:
      type: object
      required: [code, code_verifier, redirect_uri, state]
      properties:
        code:
          type: string
          minLength: 1
        code_verifier:
          type: string
          minLength: 1
        redirect_uri:
          type: string
          minLength: 1
        state:
          type: string
          minLength: 1
        nonce:
          type: string
          minLength: 1
    MobileTokenExchangeSuccess:
      type: object
      required: [token]
      properties:
        token:
          type: string
    LogoutSuccess:
      type: object
      required: [success]
      properties:
        success:
          type: boolean
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: string
    UserProfile:
      type: object
      required: [userId]
      properties:
        userId:
          type: string
        streakDays:
          type: integer
          nullable: true
        fitnessGoal:
          type: string
          nullable: true
        workoutFrequency:
          type: integer
          nullable: true
        dailySyncProgress:
          type: integer
          nullable: true
        checkInCompleted:
          type: boolean
          nullable: true
        activityImported:
          type: boolean
          nullable: true
        equipment:
          type: array
          items:
            type: string
          nullable: true
        skillLevel:
          type: string
          nullable: true
        injuries:
          type: array
          items:
            type: string
          nullable: true
        onboardingCompleted:
          type: boolean
          nullable: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
    UpdateUserProfileRequest:
      type: object
      properties:
        streakDays:
          type: integer
          nullable: true
        fitnessGoal:
          type: string
          nullable: true
        workoutFrequency:
          type: integer
          nullable: true
        dailySyncProgress:
          type: integer
          nullable: true
        checkInCompleted:
          type: boolean
          nullable: true
        activityImported:
          type: boolean
          nullable: true
        equipment:
          type: array
          items:
            type: string
          nullable: true
        skillLevel:
          type: string
          nullable: true
        injuries:
          type: array
          items:
            type: string
          nullable: true
        onboardingCompleted:
          type: boolean
          nullable: true
    CreateCheckInRequest:
      type: object
      required: [energyLevel, sleepQuality, stressLevel, sorenessScore]
      properties:
        energyLevel:
          type: integer
          minimum: 1
          maximum: 5
        sleepQuality:
          type: integer
          minimum: 1
          maximum: 5
        stressLevel:
          type: integer
          minimum: 1
          maximum: 5
        sorenessScore:
          type: integer
          minimum: 1
          maximum: 5
        soreMuscleGroups:
          type: array
          items:
            type: string
        notes:
          type: string
          nullable: true
    DailyCheckIn:
      type: object
      required: [id, userId, date, energyLevel, sleepQuality, stressLevel, sorenessScore]
      properties:
        id:
          type: integer
        userId:
          type: string
        date:
          type: string
        energyLevel:
          type: integer
        sleepQuality:
          type: integer
        stressLevel:
          type: integer
        sorenessScore:
          type: integer
        soreMuscleGroups:
          type: array
          items:
            type: string
        notes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
    CreateExternalWorkoutRequest:
      type: object
      required: [label, duration, workoutType]
      properties:
        label:
          type: string
        duration:
          type: integer
        workoutType:
          type: string
        source:
          type: string
    ExternalWorkout:
      type: object
      required: [id, userId, label, duration, workoutType]
      properties:
        id:
          type: integer
        userId:
          type: string
        label:
          type: string
        duration:
          type: integer
        workoutType:
          type: string
        source:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
